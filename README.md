# Valentine's Day App 💖

An interactive Valentine's Day proposal app built with React and Vite.

## Features
- Interactive "Yes/No" buttons with fun animations
- Growing "Yes" button mechanic
- Jumping "No" button
- Beautiful celebration page
- Mobile responsive design
- Smooth animations and effects

## Installation

```bash
npm install
npm run dev
Technologies Used
React 18

React Router DOM

Vite

CSS Animations

Live Demo
[Add your deployed link here]

Made with ❤️ for Valentine's Day

text

## Step 3: Add All Files and Commit

```bash
# Add all files
git add .

# Commit with message
git commit -m "Initial commit: Valentine's Day interactive app"

# Verify branch name
git branch -M main
Step 4: Push to GitHub
bash
git push -u origin main
Complete Command Sequence:
bash
# Configure git (one-time setup)
git config --global user.email "youremail@example.com"
git config --global user.name "Mithun-VK"

# Create README
echo "# Valentine's Day App 💖" > README.md

# Add all files
git add .

# Commit
git commit -m "Initial commit: Valentine's Day interactive app"

# Ensure main branch
git branch -M main

# Push to GitHub
git push -u origin main
Optional: Create .gitignore
Before adding files, create a .gitignore file:

bash
# Create .gitignore
echo "node_modules/
dist/
.env
*.log
.DS_Store" > .gitignore